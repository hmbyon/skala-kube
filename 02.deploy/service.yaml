apiVersion: v1
kind: Service
metadata:
  name: sk152-webserver
  namespace: class-5
  labels:
    app: sk152-webserver
spec:
  type: ClusterIP
  selector:
    app: sk152-webserver
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: mgmt
      protocol: TCP
      port: 8081
      targetPort: 8081


